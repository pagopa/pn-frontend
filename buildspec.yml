version: 0.2
batch:
  build-list:
    - identifier: dev_build
      env:
        variables:
          REACT_APP_PAGOPA_HELP_EMAIL: pn.local@assistenza.pagopa.it
          REACT_APP_URL_SELFCARE_LOGIN: https://dev.selfcare.pagopa.it/auth/login
          REACT_APP_URL_SELFCARE_BASE: https://dev.selfcare.pagopa.it
          REACT_APP_URL_API: https://webapi.dev.pn.pagopa.it/
    - identifier: uat_build
      env:
        variables:
          REACT_APP_PAGOPA_HELP_EMAIL: pn.local@assistenza.pagopa.it
          REACT_APP_URL_SELFCARE_LOGIN: https://uat.selfcare.pagopa.it/auth/login
          REACT_APP_URL_SELFCARE_BASE: ttps://uat.selfcare.pagopa.it
          REACT_APP_URL_API: https://webapi.uat.pn.pagopa.it/
phases:
  install:
    commands:
      #install Node 12
      - echo installing node
      - curl -sL https://deb.nodesource.com/setup_14.x | bash -
      - apt-get install -y nodejs
      #install yarn
      - echo installing yarn
      - curl -sS https://dl.yarnpkg.com/debian/pubkey.gpg | apt-key add -
      - echo "deb https://dl.yarnpkg.com/debian/ stable main" | tee /etc/apt/sources.list.d/yarn.list
      - apt install --no-install-recommends yarn
  pre_build:
    commands:
      # run yarn install and lerna bootstrap
      - echo launghing yarn install and lerna bootstrap
      - yarn install
  build:
    commands:
      - echo go in packages/pn-pa-webapp directory
      - cd ./packages/pn-pa-webapp
      - yarn build
artifacts:
  baseDirectory: build
  files:
    - '**/*'
cache:
  paths:
    - node_modules/**/*
