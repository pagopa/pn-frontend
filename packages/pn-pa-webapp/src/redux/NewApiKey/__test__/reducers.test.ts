import { mockAuthentication } from '../../auth/__test__/test-utils';

describe('api keys page redux state test', () => {
  mockAuthentication();

  // TO-DO: Make test for fetching data when BE is ready
  it.skip('Should be able to fetch the api keys', async () => {
    
  });
});