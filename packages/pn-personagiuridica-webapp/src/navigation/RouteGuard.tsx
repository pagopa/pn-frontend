import { Outlet } from "react-router-dom";

// TODO: evaluate if took RouteGuard from pf and pa, create a common component and put it in pn-commons
const RouteGuard = () => <Outlet />;

export default RouteGuard;
